import pandas as pd

class CandlestickPatternDetector:
    DOJI_THRESHOLD = 0.05
    SMALL_BODY_RATIO = 0.25
    LONG_SHADOW_RATIO = 2.0

    def detect_all_patterns(self, df: pd.DataFrame) -> list:
        # Placeholder for full detection
        return []
